import { Container } from "../layout/layout.styles";

const NotFound = (): JSX.Element => (
  <Container>
    <h1 style={{ textAlign: "center" }}>That page was not found.</h1>
  </Container>
);

export default NotFound;